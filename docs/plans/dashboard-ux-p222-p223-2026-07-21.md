# Dashboard UX P222–P223（2026-07-21）

## P222 — 写门禁文案统一 helper
- [x] EARS-FE-P222-01 `mutationBlockedMessageKey` / `mutationBlockedTitle` 单测
- [x] EARS-FE-P222-02 `useMutationGuard.blockedMessageKey(offlineKey)`
- [x] EARS-FE-P222-03 主写路径页面去掉重复 session/offline 三元

## P223 — API 默认请求超时
- [x] EARS-FE-P223-01 `createTimeoutSignal`（默认 30s，可与用户 signal 合并）
- [x] EARS-FE-P223-02 `request` / `apiGetText` / `probeReadyz(5s)` 接入超时
- [x] EARS-FE-P223-03 超时 → `APIClientError(408, timeout)`；`apiError` 友好文案
- [x] EARS-FE-P223-04 NDJSON 长流不强制默认超时（仅用户 abort → canceled）

### 非目标
- 服务端 refresh token
- 宣称可商用完成

### 验证
- [x] npm test / build / test:e2e
- [x] go test ./...
- [x] make e2e
