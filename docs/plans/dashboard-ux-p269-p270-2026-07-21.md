# Dashboard UX P269–P270（2026-07-21）

## P269 Query soft-fail

- `executeQuery` 不再在请求开始时清空结果
- 成功后原子替换快照；失败/取消保留上次成功结果
- `ActionResultBanner` + 可重试；有快照时 warn 文案

## P270 Write soft-fail

- 写入失败/取消不抹掉上次 `result.ok` 成功提示
- `write-action-error` 可重试；有成功快照时 warn 文案

## 验收

- npm test / build / e2e
