# Dashboard UX P280–P281（2026-07-21）

## 目标
- P280：强制改密页密码可见性切换（与登录页一致）
- P281：会话临界倒计时高频刷新（critical/expired 1s，正常 15s）

## 验收
- [x] force-old/new/confirm 具备 toggle，type 可在 password/text 间切换
- [x] `sessionClockTickMs` 单测覆盖
- [x] useMutationGuard / Account 会话时钟自适应间隔
- [x] commercial-smoke 覆盖 force toggle
- [x] npm test / build / e2e 通过后合入

## 实现备注
- 抽取 `sessionClockTickMs` 纯函数，避免多处硬编码
- useMutationGuard 用 `createSessionClock` 保持主函数精简
