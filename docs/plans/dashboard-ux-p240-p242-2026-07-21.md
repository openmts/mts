# Dashboard UX P240–P242（2026-07-21）

## P240 — Overview 分项失败可重试 + ApiSpec 重试
- [x] `adminPartialError` 支持 retryable + dismiss + `overview-partial-error`
- [x] ApiSpec `loadError` 改 ActionResultBanner + retry

## P241 — Storage 列表失败可见可重试
- [x] `loadSnapshots`/`loadDataSnapshots` 不再静默吞错
- [x] `storage-list-error` + `reloadStorageLists`

## P242 — 命令面板深链补齐
- [x] `/storage#snapshots`
- [x] `/account#account-session|account-password|account-density`
- [x] i18n + 单测

## 非目标
- 宣称可商用完成
- 服务端 refresh token / 部署侧实装
