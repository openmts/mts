# Dashboard UX P278–P279（2026-07-21）

## 目标
- P278：会话 critical 写门禁 e2e 覆盖剩余时间与 write-submit title
- P279：登录/强改密错误区可恢复（retry/dismiss），本地校验不暴露无意义 retry

## 验收
- [x] `session-critical-remaining` 在 admin/non-admin critical 路径可见，恢复后消失
- [x] write critical 下 submit disabled，title 非 offline
- [x] 登录空密码：error alert + dismiss，无 retry
- [x] 登录错误密码：error + retry + dismiss
- [x] 强改密本地策略失败：error + dismiss，无 retry
- [x] `npm test` / `npm run build` / `npm run test:e2e` 通过后合入

## 实现备注
- LoginPage / ForceChangePasswordPage：`errorRetryable` 区分本地与可重试失败
- commercial-smoke 对齐上述语义，避免 dismiss 后仍 assert role
