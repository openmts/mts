# Dashboard UX P232（2026-07-21）

## 目标
取消类 toast 从 success（绿色）改为 info，与取消语义一致。

## EARS
- [x] EARS-FE-P232-01 导出取消 `exportCancelledToast` 使用 `info`
- [x] EARS-FE-P232-02 查询/写入取消使用 `info`
- [x] EARS-FE-P232-03 各页 `useNotify` 解构包含 `info`

## 非目标
- 改变成功导出 toast
- 宣称可商用完成
