# 代码检视报告：Wide 布局 Write vs WriteTypedBatch 与列式批量写 API

- 日期：2026-07-18 12:56 +0800
- 范围：公开写路径 API、bench 对比、`PointsToTypedBatch` / `WritePointsAsTypedBatch`

## 对比结论（同一 wide 字段布局，points=10000, count=8）

| 路径 | ns/op median | B/op median | allocs/op median |
|---|---:|---:|---:|
| Write (WideBatch) | ~41.9ms | ~48.3MB | ~136342 |
| WriteTypedBatch (TypedWideBatch) | ~14.6ms | ~6.53MB | ~16247 |

- 时延：Typed 约为 Write 的 **35%**（约 **2.9x** 更快）
- 分配次数：下降约 **88%**
- 分配字节：下降约 **86%**

判定：收益显著，落地列式转换与批量写封装。

## 三路对比（points=10000, count=8, Sync=true）

| 路径 | ns/op median | B/op median | allocs/op median | 相对 Write 时延 |
|---|---:|---:|---:|---:|
| `Write` (WideBatch) | ~41.6ms | ~48.3MB | ~136343 | 100% |
| `WritePointsAsTypedBatch` | ~26.5ms | ~23.2MB | ~76292 | ~63.6%（约 1.57x） |
| `WriteTypedBatch` 直接列式 | ~14.6ms | ~6.54MB | ~16248 | ~35.0%（约 2.86x） |

解读：
- 直接 `WriteTypedBatch` 仍最优（无 `[]Point`/map 构造与转换）。
- `WritePointsAsTypedBatch` 介于两者之间：比 `Write` 明显更好，但比原生列式多一层 `PointsToTypedBatch` 成本。
- 业务若已持有 `[]Point` 且字段同构，优先 `WritePointsAsTypedBatch`；新采集路径建议直接构造 `TypedBatch`。


## 实现项

1. `PointsToTypedBatch([]Point) (TypedBatch, error)`：同构 batch 转列式
2. `Engine.WritePointsAsTypedBatch`：转换后走 `WriteTypedBatch`
3. bench：`makeWideBenchTypedBatch` + `BenchmarkEngineWriteTypedWideBatch`
4. 文档：README 批量写入补充转换示例

## 约束（转换）

- Database/RP/Measurement/Precision 整批一致
- field 集合完全一致，类型不可冲突
- 不支持稀疏 field
- tag key 取并集，缺失填空串
- 返回新分配，不借用输入 map

## 验证

- 定向单测：PointsTo / WritePointsAsTypedBatch 通过
- `make test` 通过
- `make e2e` 通过
- `make lint` 0 issues
- 核心 race 定向通过

## 状态

- [x] 对比完成
- [x] API 实现
- [x] 测试与文档
