# Task 6 规格与质量联合检视报告

- 检视范围：`git diff 3cef174..5931947`
- 检视日期：2026-06-24 13:32
- 检视结论：✅ Approved（通过）

## 改动概览

| 文件 | 变更 |
| --- | --- |
| internal/engine/lifecycle.go | +5 |
| internal/engine/shard.go | +14 -1 |
| internal/service/admin.go | +3 |
| internal/service/logging_test.go | +34 |
| internal/service/server.go | +1 |

## 规格逐项核验

### 1. shard.go openParts WARN 日志 ✅

- `partOpenRecoveryIssue`：`shard.go:543-548` 输出 WARN，属性含 `shard`、`part`、`kind`、`error`，完全符合规格。
- `partMetadataMismatchIssue`：`shard.go:553-557` 输出 WARN，属性含 `shard`、`part`、`message`。
  - 说明：metadata mismatch 的 `RecoveryIssue` 结构体无 `Err` 字段（仅 `Message`），且 `Kind` 恒为 `RecoveryIssueMetadataMismatch`，故使用 `message` 传递具体不匹配字段更富信息量。属合理偏差，非阻塞。

### 2. lifecycle.go preflightCompactionDiskSpace WARN 日志 ✅

- `lifecycle.go:309-314` 在返回 `ErrCompactionDiskSpaceExceeded` 之前输出 WARN，属性含 `shard`、`available_bytes`、`required_bytes`，符合规格。

### 3. admin.go compactHandler logger 参数 ✅

- `admin.go:40` 新增 `logger *slog.Logger` 参数。
- `admin.go:52` 鉴权失败时输出 `logger.Warn("admin auth failed", "remote_addr", request.RemoteAddr)`，符合规格。

### 4. server.go 传递 logger ✅

- `server.go:56` 在构造 `compactHandler` 时传入 `logger`，符合规格。

### 5. admin 鉴权失败日志测试 ✅

- `logging_test.go:151-180` `TestAdminAuthFailureLogs`：构造无 token 请求，断言返回 401 且日志包含 `admin auth failed`。

### 6. 回归测试 ✅

```
ok  github.com/openmts/mts/internal/engine   0.357s  coverage: 90.1%
ok  github.com/openmts/mts/internal/service  0.005s  coverage: 96.0%
```

## 质量核验

### 覆盖率

- engine：90.1%（≥90% ✅）
- service：96.0%（≥90% ✅）

新增日志行覆盖情况：
- `shard.go` 两条 WARN 由 `recovery_audit_test.go` 中 `TestOpenShardReturnsRecoveryFatalForMissingManifestPart` 与 `TestOpenShardReturnsRecoveryFatalForManifestMetadataMismatch` 覆盖。
- `lifecycle.go` WARN 由 `compaction_preflight_test.go` 中 `TestCompactionRejectsWhenDiskSpaceInsufficient` 覆盖。
- `admin.go` WARN 由新增 `TestAdminAuthFailureLogs` 覆盖。

### Lint

- `golangci-lint run ./internal/engine/... ./internal/service/...`：0 issues ✅

## 风险与建议

- 非阻塞建议：`partMetadataMismatchIssue` 的 WARN 日志可补充 `kind` 属性以与 `partOpenRecoveryIssue` 保持结构一致，便于日志检索过滤。当前 `message` 已包含不匹配字段详情，功能上无影响。

## 问题处理状态

本次检视未发现需修复问题，所有规格项与质量门禁均通过。
